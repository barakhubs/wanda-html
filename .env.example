# Application Environment
APP_ENV=production
APP_DEBUG=false
APP_URL=https://wandacommunications.com

# Database
DB_HOST=localhost
DB_PORT=3306
DB_NAME=wanda_db
DB_USER=db_user
DB_PASS=strong_password_here
DB_CHARSET=utf8mb4
DB_PERSISTENT=false

# Session
SESSION_NAME=wanda_session

# Upload limits (bytes — 5 MB default)
MAX_UPLOAD_BYTES=5242880
